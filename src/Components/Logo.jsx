import React from "react";

const Logo = () => {
  return (
    <div className="uppercase bg-[conic-gradient(at_right,_var(--tw-gradient-stops))] from-fuchsia-800 via-red-400 to-blue-500 bg-clip-text text-transparent text-7xl font-bold">
      ChatWave
    </div>
  );
};

export default Logo;
